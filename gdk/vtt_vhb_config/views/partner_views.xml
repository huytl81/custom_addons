<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <!--Partner views-->
    <data>
        <!--Search view-->
        <record id="vtt_view_search_res_partner_users" model="ir.ui.view">
            <field name="name">Partner Filter Users</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_res_partner_filter"/>
            <field name="arch" type="xml">
                <filter name="inactive" position="before">
                    <filter string="Internal Users" name="filter_users" domain="[('user_ids', '!=', False)]"/>
                    <separator/>
                </filter>
            </field>
        </record>
    </data>
</odoo>